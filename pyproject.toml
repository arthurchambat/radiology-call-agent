[project]
name = "rounded-radiology-agent"
version = "0.1.0"
description = "Simple tools for a radiology voice agent technical test"
requires-python = ">=3.9"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27"
]

[tool.uv]
package = false
